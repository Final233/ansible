- name: this is test
  shell: echo {{ test_var }}

- command: whoami
  register: result
- debug:
    var: result.stdout

- name: test
  shell: echo {{ ansible_date_time.date }} {{ ansible_date_time.time }}
  register: result
- debug:
    var: result.stdout

- name: test1
  shell: ls perl-ExtUtils-Embed